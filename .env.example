# Update these with your Supabase details from your project settings > API
# https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key

# Server-only secret key (service role). Used by DNS verify + check-failed cron.
# Never expose this to the browser.
SUPABASE_SECRET_KEY=your-supabase-secret-or-service-role-key

# Shared secret for Vercel Cron (check-failed). Vercel sends Authorization: Bearer <CRON_SECRET>
CRON_SECRET=generate-a-long-random-string
